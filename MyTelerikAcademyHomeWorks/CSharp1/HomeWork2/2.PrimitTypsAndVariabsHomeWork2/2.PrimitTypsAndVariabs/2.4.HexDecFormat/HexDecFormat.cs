using System;

class HexDecFormat
{
    static void Main()
    {
        byte hexDecNum = 0xFE;
        Console.WriteLine("Integer variable {0} in hexadecimal format: {0:X}", hexDecNum, hexDecNum);
    }
}

