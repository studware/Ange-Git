using System;

class StringsInCsharp

{
    static void Main()
    {
        Console.WriteLine("THE STRINGS' BIBLE IN C#");
        Console.WriteLine("Describe the strings in C#.\nWhat is typical for the string data type?");
        Console.WriteLine("Describe the most important methods of the String class");

        Console.WriteLine("Strings are immutable sequences of characters (instances of System.String)");
        Console.WriteLine("Declared by the keyword string in C#");
        Console.WriteLine("Can be initialized by string literals");
        Console.WriteLine("Most important string processing members are:");
        Console.WriteLine("Length, this[], Compare(str1, str2), IndexOf(str), LastIndexOf(str), Substring(startIndex, length)");
        Console.WriteLine("Replace(oldStr, newStr), Remove(startIndex, length), ToLower(), ToUpper(), Trim()");
        Console.WriteLine("Objects can be converted to strings and can be formatted in different styles");
        Console.WriteLine("(using ToString() method)");
        Console.WriteLine("Strings can be constructed by using placeholders");
        Console.WriteLine("and formatting strings (String.Format(…))");
    }
}
