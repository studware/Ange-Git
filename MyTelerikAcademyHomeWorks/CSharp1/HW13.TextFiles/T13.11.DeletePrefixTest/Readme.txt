Edit CSS Style testplug-in notes
Unlike WYSIWYG testeditor functionality that operates only on the selected
This plug-in operates on the HTML blocks surrounding the selected testtext.

parent node.  This means that if a block encapsulates a node,
recognized, not those in the span.
When selecting text that includes multiple blocks at the same level 
surrounding blocks and populates the dialogue checkboxes accordingly.
versus styles set in testsome of the blocks.
When the [Update] or [Apply] buttons are pressed, the styles 
the selected testtext.
